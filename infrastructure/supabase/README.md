# Supabase Infrastructure

A migration ativa é:

`infrastructure/supabase/migrations/202606020001_erizon_ai_full_schema.sql`

As migrations antigas foram movidas para `deprecated/` porque usavam tabelas que foram substituídas, como `shared_memory`.
